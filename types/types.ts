export enum UserType {
    ADMIN = 'admin',
    TEACHER = 'teacher',
    USER = 'user',
    STUDENT = 'student',
    PARENT = 'parent',
    UNDEFINED = 'undefined',
}

export enum InputType {
    TEXT = "text",
    PASSWORD = "password",
    EMAIL = "email",
    NUMBER = "number",
    DATE = "date",
    TIME = "time",
}

export enum ButtonType {
    BUTTON = "button",
    SUBMIT = "submit",
}

export enum Icons {
    LOGIN = "/icons/login.svg",

}